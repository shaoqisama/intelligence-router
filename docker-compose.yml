services:
  intelligence-router:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
      - ./.data:/app/.data
    restart: unless-stopped
